Analyze the following git diff and generate a commit message. The commit message should follow conventional commits format (e.g., "feat: add user authentication", "fix: resolve null pointer in handler", "docs: update API documentation").

Output format:
First line: the commit title (max 72 chars, type: description)
Then a blank line
Then the commit body with details about what changed and why.

Git diff:
{{diffOutput}}