{
  "name": "server",
  "private": true,
  "scripts": {
    "codver": "bun run codver.ts",
    "clean": "bun run codver.ts clean",
    "test": "bun test",
    "test:watch": "bun test --watch"
  },
  "dependencies": {
    "@earendil-works/pi-ai": "latest",
    "@earendil-works/pi-coding-agent": "latest",
    "commander": "^14.0.3",
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}